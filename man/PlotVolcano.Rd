% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PlotVolcano.R
\name{PlotVolcano}
\alias{PlotVolcano}
\title{Volcano plot for DE results}
\usage{
PlotVolcano(
  de,
  fc_col = NULL,
  p_col = NULL,
  gene_col = NULL,
  fc_threshold = 1,
  p_threshold = 0.05,
  top_n = 20,
  label_genes = NULL,
  colors = c("grey70", "#D6604D", "#4393C3"),
  point_size = 1,
  label_size = 3,
  title = NULL
)
}
\arguments{
\item{de}{A data frame of DE results with, at minimum, a log2-fold-change
column and an adjusted p-value column.}

\item{fc_col}{Name of the log2 fold change column. \code{NULL} (default)
auto-detects among \code{"log2FC"}, \code{"avg_log2FC"},
\code{"logFC"}, \code{"log2FoldChange"}.}

\item{p_col}{Name of the adjusted p-value column. \code{NULL} (default)
auto-detects among \code{"padj"}, \code{"p_val_adj"}, \code{"FDR"},
\code{"adj.P.Val"}.}

\item{gene_col}{Name of the gene-symbol column. \code{NULL} (default)
auto-detects among \code{"gene"}, \code{"Gene"}, \code{"gene_symbol"}
and falls back to rownames.}

\item{fc_threshold}{Absolute log2FC threshold for the "significant"
coloring / labeling. Default 1 (2-fold change).}

\item{p_threshold}{Adjusted p-value threshold. Default 0.05.}

\item{top_n}{Number of top genes (by combined significance + effect
size) to label. Default 20. Set to 0 to disable labeling.}

\item{label_genes}{Character vector of specific genes to label
regardless of significance. Combined with the top-N selection.}

\item{colors}{Length-3 vector of colors: c(non-significant, up, down).
Default \code{c("grey70", "#D6604D", "#4393C3")}.}

\item{point_size}{Base point size. Default 1.}

\item{label_size}{Text size for the labeled gene names. Default 3.}

\item{title}{Plot title. \code{NULL} auto-generates one from the DE
metadata if present (\code{contrast_num}, \code{contrast_den},
\code{cluster} columns).}
}
\value{
A \code{ggplot} object.
}
\description{
Standard volcano plot with configurable significance / effect
thresholds, top-gene labeling, and automatic column-name detection so
it accepts either \code{FindMarkers}-style output (\code{avg_log2FC},
\code{p_val_adj}) or \code{PseudobulkDE}-style output (\code{log2FC},
\code{padj}).
}
\examples{
\dontrun{
# From PseudobulkDE
res <- PseudobulkDE(obj, sample_col = "orig.ident",
                    condition_col = "treatment",
                    ident_1 = "drug", ident_2 = "vehicle")
PlotVolcano(res$results)

# From FindMarkers
fm <- FindMarkers(obj, ident.1 = "cluster1", ident.2 = "cluster2")
PlotVolcano(fm, gene_col = NULL)   # uses rownames

# Custom labeling
PlotVolcano(res$results,
            label_genes  = c("MYC", "TP53"),
            fc_threshold = 1.5,
            top_n        = 10)
}
}
