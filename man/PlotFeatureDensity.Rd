% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PlotFeatureDensity.R
\name{PlotFeatureDensity}
\alias{PlotFeatureDensity}
\title{Nebulosa-style feature density on a dimensional reduction}
\usage{
PlotFeatureDensity(
  obj,
  features,
  reduction = "umap",
  dims = c(1, 2),
  assay = NULL,
  layer = "data",
  n_grid = 200,
  joint = FALSE,
  colors = c("lightgrey", "#FEB24C", "#F03B20", "#BD0026"),
  ncol = NULL,
  pt_size = 0.3
)
}
\arguments{
\item{obj}{A Seurat object with the reduction embeddings computed.}

\item{features}{Character vector of gene names or metadata columns to
plot. Genes are pulled from \code{assay}'s \code{"data"} layer;
anything in \code{obj@meta.data} is used directly.}

\item{reduction}{Reduction name. Default \code{"umap"}.}

\item{dims}{Which two reduction dimensions to plot. Default
\code{c(1, 2)}.}

\item{assay}{Assay for gene lookups. Default DefaultAssay.}

\item{layer}{Layer to read expression from. Default \code{"data"}.}

\item{n_grid}{Grid resolution for the KDE. Default 200 (higher =
smoother, slower).}

\item{joint}{If TRUE, plot a "joint" density combining all requested
features (co-expression signal) as an additional panel. Default FALSE.}

\item{colors}{Character vector of colors for the density gradient.
Default \code{c("lightgrey", "#FEB24C", "#F03B20", "#BD0026")}.}

\item{ncol}{Number of columns in the facet grid. Default \code{NULL}
(ggplot picks).}

\item{pt_size}{Size of the underlying scatter of cells (drawn faintly
beneath the density). Default 0.3. Set to 0 to hide.}
}
\value{
A \code{ggplot} object.
}
\description{
Plots a 2D kernel-density estimate of expression (or module scores) on
the specified reduction (usually UMAP), one facet per feature. Weighted
KDE by expression makes sparse markers much easier to see than the
standard \code{Seurat::FeaturePlot}, which just draws grey-to-blue
points and loses signal in dense regions.
}
\details{
Implemented with \code{ks::kde} weighted by the feature's expression at
each cell. Falls back to \code{MASS::kde2d} weighted by a threshold cut
if \code{ks} is unavailable.
}
\examples{
\dontrun{
PlotFeatureDensity(obj, features = c("CD3D", "CD4", "CD8A"))
PlotFeatureDensity(obj, features = c("CD3D", "CD4"), joint = TRUE)
PlotFeatureDensity(obj, features = "module_score_Bcell",
                   reduction = "harmony")
}
}
