% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/NicheCoExpress.R
\name{plotNicheCoExpress}
\alias{plotNicheCoExpress}
\title{Visualise the output of nicheCoExpress}
\usage{
plotNicheCoExpress(
  res,
  type = c("heatmap", "scores"),
  pairs = NULL,
  niches = NULL,
  top_n = 30,
  sig_levels = c(`***` = 0.001, `**` = 0.01, `*` = 0.05)
)
}
\arguments{
\item{res}{The list returned by \code{\link{nicheCoExpress}}.}

\item{type}{\code{"heatmap"} or \code{"scores"}.}

\item{pairs}{(scores mode) character vector of pair IDs
(\code{"geneA_geneB"}) to show; default = top \code{top_n} most
significant.}

\item{niches}{Optional subset of niches to display.}

\item{top_n}{In heatmap: max pairs to show, ranked by smallest p_adj;
in scores: number of top pairs if \code{pairs} is NULL.}

\item{sig_levels}{Named thresholds for significance stars.}
}
\value{
A \code{ggplot} object.
}
\description{
Two views:
\describe{
\item{\code{type = "heatmap"} (default)}{Niche (rows) by gene-pair
(cols) heatmap of \code{delta_log2} — the change in
co-expression between the two conditions (test minus reference).
Diverging palette centred at 0: red = gained co-expression in the
test condition, blue = lost. Significance stars are drawn from
\code{p_adj}.}
\item{\code{type = "scores"}}{Per-sample co-expression scores for
one or more gene pairs, split by condition and faceted by niche.
Box + jitter so you can see the replicate-level spread the
statistics are testing.}
}
}
\seealso{
\code{\link{nicheCoExpress}}
}
