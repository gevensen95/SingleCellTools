% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/combine_metadata.R
\name{combine_metadata}
\alias{combine_metadata}
\title{Combine per-cell metadata from a list of Seurat objects}
\usage{
combine_metadata(seurat_list, sample_col = "sample", cell_col = "cell_id")
}
\arguments{
\item{seurat_list}{A list of Seurat objects.}

\item{sample_col}{Name of the column in the result that records which list
element each row came from. Default \code{"sample"}.}

\item{cell_col}{Name of the column that holds the original cell barcode.
Default \code{"cell_id"}.}
}
\value{
A tibble with all per-cell metadata rows from every input object.
}
\description{
Stacks the \code{@meta.data} of every Seurat object in a list into a single
long-format data frame, with the original list name carried in a
\code{sample} column and the original cell barcode in a \code{cell_id}
column. Tolerates objects with different metadata columns
(missing ones become \code{NA}) via \code{dplyr::bind_rows}.
}
