% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/strip_workflow_artifacts.R
\name{strip_workflow_artifacts}
\alias{strip_workflow_artifacts}
\title{Strip normalized layers, variable features, and reductions}
\usage{
strip_workflow_artifacts(obj, assay = "RNA", keep_reductions = NULL)
}
\arguments{
\item{obj}{A Seurat object OR a (optionally named) list of Seurat objects.}

\item{assay}{Assay whose layers to strip. Default \code{"RNA"}.}

\item{keep_reductions}{Character vector of reduction names to \strong{preserve}.
Everything else is dropped. Default \code{NULL} drops all reductions.
Example: \code{keep_reductions = "harmony"} keeps harmony but drops
\code{pca}, \code{umap}, \code{tsne}, etc.}
}
\value{
The same shape as the input.
}
\description{
Removes the intermediate artifacts that normalization + PCA + UMAP +
integration leave on a Seurat object: the \code{data} and
\code{scale.data} layers, the variable-features set, and \strong{every}
reduction (\code{pca}, \code{umap}, \code{harmony}, \code{tsne}, ...).
By default the result carries only counts plus metadata.
}
\details{
Handles single objects or lists, v5 split layers (\code{data.1},
\code{scale.data.2}, ...), and assay-suffixed reductions
(\code{pca.RNA}, \code{umap.SCT}).
}
