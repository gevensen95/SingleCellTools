% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AddGenePositivity.R
\name{AddGenePositivity}
\alias{AddGenePositivity}
\title{Add per-gene positivity metadata to one or more Seurat objects}
\usage{
AddGenePositivity(
  seurat_objects,
  genes,
  assay = NULL,
  layer = "counts",
  threshold = 0,
  suffix = "_pos"
)
}
\arguments{
\item{seurat_objects}{A Seurat object, or a (optionally named) list of them.}

\item{genes}{Character vector of gene symbols to score.}

\item{assay}{Assay to read from. Defaults to each object's DefaultAssay.}

\item{layer}{Layer to read ('counts', 'data', ...). Default 'counts'.}

\item{threshold}{Numeric; cells with expression > threshold are positive.
Use the default (0) for counts; bump it (e.g. 1) for log-normalized data.}

\item{suffix}{String appended to gene name for the metadata column.
Gene 'CD3D' with default suffix becomes column 'CD3D_pos'.}
}
\value{
The same shape as \code{seurat_objects} (single object or list) with new
logical metadata columns added.
}
\description{
For every gene in \code{genes}, adds a logical metadata column to the Seurat
object (or every Seurat object in a list) indicating whether each cell has
expression strictly greater than \code{threshold} in the specified assay/layer.
}
\details{
Behavior:
\itemize{
\item \code{seurat_objects} may be a single Seurat object OR a list of them — the
return type matches the input shape.
\item \code{genes} is pre-filtered to the genes actually present in at least one of
the supplied objects; genes missing from every object are dropped with a
warning. Within an individual object, any remaining genes that don't exist
in that particular object are filled with \code{NA} in its metadata.
}
}
