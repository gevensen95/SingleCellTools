% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_polygon_coords.R
\name{get_polygon_coords}
\alias{get_polygon_coords}
\title{Extract per-vertex cell segmentation polygon coordinates}
\usage{
get_polygon_coords(obj, image, meta_cols = NULL)
}
\arguments{
\item{obj}{A Seurat object.}

\item{image}{Name of the image/FOV in \code{obj@images} to extract from.}

\item{meta_cols}{Optional character vector of \code{obj@meta.data} columns to
join onto the output (e.g. a cell-type column, or a gene's expression
pulled in beforehand with \code{obj$gene <- Seurat::FetchData(obj, "gene")[[1]]}). \code{NULL} (default) returns just the coordinates.}
}
\value{
A data.frame with columns \code{cell}, \code{x}, \code{y} (one row per polygon
vertex, in vertex order), plus any \code{meta_cols} requested.
}
\description{
Pulls the segmentation-boundary polygon vertices for every cell in a
spatial FOV/image (Xenium, CosMx, MERFISH -- any Seurat object whose
image has a \code{"segmentation"} boundary set, as opposed to only cell
centroid points), optionally joined with metadata columns for downstream
coloring. Companion to \code{\link[=PlotPolygons]{PlotPolygons()}}, which plots the result.
}
\details{
\code{Seurat::GetTissueCoordinates(obj[[image]], which = "segmentation")}
returns one row per polygon VERTEX (many rows per cell), unlike the
one-row-per-cell centroid coordinates \code{\link[=get_all_coords]{get_all_coords()}} collects -- so
this is deliberately a separate function rather than a
\code{which = "segmentation"} option bolted onto that one. Joining metadata by
cell also has to replicate each cell's metadata across all of its vertex
rows, which \code{get_all_coords()}'s row-per-cell logic doesn't do.
}
\examples{
\dontrun{
poly <- get_polygon_coords(xenium, image = "fov1", meta_cols = "cell_type")
PlotPolygons(poly, feature = "cell_type")
}

}
\seealso{
\code{\link[=PlotPolygons]{PlotPolygons()}}, \code{\link[=stack_polygons]{stack_polygons()}}
}
